# Ensure platform-agnostic installations
optional=false
# Use the correct SWC binary for Vercel
swc_platform=linuxmusl-x64
# Force npm to use the correct platform
platform=linux
# Ensure devDependencies are installed in production
production=false
# Handle peer dependency issues
legacy-peer-deps=true
# Ensure all dependencies are installed
include=dev
# Use npm's legacy peer deps to handle peer dependency issues
legacy-peer-deps=true
